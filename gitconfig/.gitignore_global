# グローバルな .gitignore
# ここに ignore の対象を追加するときは、各プロジェクトに影響を与えないかよく吟味してから追加すること！
# 例えば、.env をここで追加してしまうと、チーム開発の際に .env の ignore を忘れて機密情報を push してしまう可能性がある。なので、チーム開発では各プロジェクトの .gitignore に .env を追加する。
.DS_Store
.envrc
